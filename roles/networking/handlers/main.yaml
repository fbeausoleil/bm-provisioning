- name: dm0_service
  ansible.builtin.service:
    name: create-dm0.service
    state: restarted
    enabled: true
  when: not ansible_check_mode

- name: start netplan
  ansible.builtin.command:
    cmd: netplan apply
  when: not ansible_check_mode
  
  
